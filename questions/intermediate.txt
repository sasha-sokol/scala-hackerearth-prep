1. Variables declared with `val` cannot be changed after initialisation.
2. An instance of Option[T] contains either some instance of type T, or a special value, None.
    It is used when a method may fail to return an instance of T.
